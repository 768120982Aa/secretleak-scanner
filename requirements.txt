# SecretLeak Scanner — Python Dependencies
# Python >= 3.9 required

# Core scanning (no heavy dependencies, pure stdlib where possible)
# scanner.py itself only needs: re, json, os, sys, argparse, subprocess, pathlib

# Optional: for richer CLI output formatting
# colorama>=0.4.6

# Optional: for YAML pattern file support (future)
# pyyaml>=6.0

# Testing
pytest>=7.4.0
pytest-cov>=4.1.0

# Type checking
mypy>=1.5.0

# Linting
ruff>=0.1.0
